name: orbixhub_front
description: "OrbixHub — app de gestão multi-tenant e modular."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ">=3.12.0 <4.0.0"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.3.1
  go_router: ^17.3.0
  dio: ^5.9.2
  freezed_annotation: ^3.1.0
  json_annotation: ^4.12.0
  flutter_secure_storage: ^10.3.1
  google_fonts: ^8.1.0
  shared_preferences: ^2.5.5
  file_picker: ^11.0.2
  printing: ^5.15.0
  pdf: ^3.13.0
  socket_io_client: ^3.0.0
  audioplayers: ^6.1.0
  web: ^1.1.0
  fl_chart: ^1.1.1
  csv: ^6.0.0
  archive: ^4.0.7
  animations: ^2.0.11
  intl: ^0.20.2
  url_launcher: ^6.3.2
  share_plus: ^10.1.3

  # Offline-first: DB local, conectividade e cripto (só usados em !kIsWeb)
  drift: ^2.28.0
  sqlcipher_flutter_libs: ^0.6.5
  # Direto (não só transitivo): precisamos de `open.overrideFor` no macOS/iOS
  # para forçar o SQLCipher em vez do sqlite3 puro do bundle.
  sqlite3: ^2.9.0
  connectivity_plus: ^7.0.0
  uuid: ^4.5.1
  path_provider: ^2.1.5
  path: ^1.9.0
  hashlib: ^2.4.2

dependency_overrides:
  # file_picker 11.0.2 pula kotlin-android para AGP 9+ (espera builtInKotlin=true).
  # Usamos builtInKotlin=false para compat com audioplayers/sodium/share_plus,
  # então usamos cópia local com build.gradle corrigido.
  file_picker:
    path: packages/file_picker

dev_dependencies:
  flutter_test:
    sdk: flutter
  # Testes que precisam do dispositivo real (canais de plataforma). Não entram
  # em `flutter test` — rodam com `flutter test integration_test/... -d <alvo>`.
  integration_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  fake_async: ^1.3.3
  build_runner: ^2.15.0
  freezed: ^3.2.5
  json_serializable: ^6.14.0
  mocktail: ^1.0.5
  drift_dev: ^2.28.0
  flutter_launcher_icons: ^0.14.4
  image: ^4.9.1

# Ícone do app em todas as plataformas. As duas imagens abaixo NÃO são editadas
# à mão: saem de `dart run tool/build_app_icons.dart`, que as deriva da arte
# oficial da marca em `tool/icon/orbix-logo-source.png`.
#
# Ao trocar a arte, rode nesta ordem:
#   dart run tool/build_app_icons.dart
#   dart run flutter_launcher_icons
flutter_launcher_icons:
  image_path: "tool/icon/icon_master.png"

  android: true
  # O adaptive icon do Android recorta a imagem, então ele recebe o glyph solto
  # sobre o navy da marca — e não o card arredondado, que seria mascarado duas vezes.
  adaptive_icon_background: "#1E2245"
  adaptive_icon_foreground: "tool/icon/icon_foreground.png"
  # Ícone monocromático (tema dinâmico do Android 13+).
  adaptive_icon_monochrome: "tool/icon/icon_foreground.png"

  ios: true
  # iOS rejeita PNG com canal alpha; o master já é opaco, mas manter o flag
  # protege caso a arte de origem volte a ter transparência.
  remove_alpha_ios: true

  web:
    generate: true
    background_color: "#1E2245"
    theme_color: "#1E2245"

  windows:
    generate: true
    icon_size: 256

  macos:
    generate: true

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/sounds/notify.wav

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
